# 本地 npm run dev 时生效（Vite development 模式）
# 连接队友后端（内网穿透）；若需改回本机，可建 .env.development.local 覆盖本文件
# 路径需含 /api，与 request 层中的 endpoint（如 /auth/login）拼接为完整 URL
 VITE_API_BASE_URL=https://5561652c.r37.cpolar.top/api
# VITE_API_BASE_URL=http://localhost:8080/api
