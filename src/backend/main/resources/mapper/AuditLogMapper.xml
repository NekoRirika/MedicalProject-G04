<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xycy.chestimaging.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="com.xycy.chestimaging.model.AuditLog">
        <id column="id" property="id"/>
        <result column="operation_time" property="operationTime"/>
        <result column="operator" property="operator"/>
        <result column="operation_type" property="operationType"/>
        <result column="operation_content" property="operationContent"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="status" property="status"/>
        <result column="details" property="details"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM audit_log WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM audit_log ORDER BY operation_time DESC
    </select>

    <insert id="insert" parameterType="com.xycy.chestimaging.model.AuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (operation_time, operator, operation_type, operation_content, ip_address, status, details) 
                VALUES (#{operationTime}, #{operator}, #{operationType}, #{operationContent}, #{ipAddress}, #{status}, #{details})
    </insert>

    <update id="update" parameterType="com.xycy.chestimaging.model.AuditLog">
        UPDATE audit_log SET operation_time = #{operationTime}, operator = #{operator}, 
                operation_type = #{operationType}, operation_content = #{operationContent}, 
                ip_address = #{ipAddress}, status = #{status}, details = #{details} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM audit_log WHERE id = #{id}
    </delete>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM audit_log
    </select>

    <select id="findByCondition" resultMap="BaseResultMap">
        SELECT * FROM audit_log 
        <where>
        <if test="operator != null">AND operator LIKE CONCAT('%', #{operator}, '%')</if>
        <if test="operationType != null">AND operation_type = #{operationType}</if>
        <if test="startTime != null">AND operation_time &gt;= #{startTime}</if>
        <if test="endTime != null">AND operation_time &lt;= #{endTime}</if>
        </where>
        ORDER BY operation_time DESC 
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByCondition" resultType="java.lang.Long">
        SELECT COUNT(*) FROM audit_log 
        <where>
        <if test="operator != null">AND operator LIKE CONCAT('%', #{operator}, '%')</if>
        <if test="operationType != null">AND operation_type = #{operationType}</if>
        <if test="startTime != null">AND operation_time &gt;= #{startTime}</if>
        <if test="endTime != null">AND operation_time &lt;= #{endTime}</if>
        </where>
    </select>

</mapper>